persistence true
persistence_location /mosquitto/data/

listener 1883 0.0.0.0

# Bootstrap mode only. Docker publishes this listener on host loopback by
# default. Do not expose it to LAN/WAN before authentication and TLS exist.
allow_anonymous true
